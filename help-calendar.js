// const getHighlightedDates = (startDate, endDate) => {
//   let datesToHighlight = [];

//   if (endDate) {
//     datesToHighlight.push(
//       addDays(startDate, -2),
//       addDays(startDate, -1),
//       addDays(endDate, 1),
//       addDays(endDate, 2)
//     );
//   } else {
//     datesToHighlight.push(
//       addDays(startDate, -2),
//       addDays(startDate, -1),
//       addDays(startDate, 1),
//       addDays(startDate, 2)
//     );
//   }

//   return datesToHighlight;
// };

// const highlightedDates = getHighlightedDates(startDate, endDate);

// highlightDates={highlightedDates}

// ==================================
