import { StyledWrapMainContent } from './WrapMainContent.styled';

const WrapMainContent = ({ children }) => {
  return <StyledWrapMainContent>{children}</StyledWrapMainContent>;
};

export default WrapMainContent;
